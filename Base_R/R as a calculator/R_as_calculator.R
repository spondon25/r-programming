# R as a calculator

#addition
2 + 4
#division
10/3
#multiplication
2 * 4

# Similar to a calculator, there are symbols for other arithmetic operations.
# E.g. -(subtraction), /(division), *(multiplication), etc.

((3 + 12)/3 + 8)
##Parentheses "(" and ")" are used to define arithmetic expressions, and they must be matched; unmatched parentheses will result in error.

{10 + 2}+ 5
##Using curly brackets "{" and "}" do not result in any error but should not be used as they have some specific uses in R, e.g. defining a function.

##The square brackets "[" and "]" cannot be used in arithmetic expressions.
